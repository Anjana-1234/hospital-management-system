# Server
PORT=

# MongoDB connection string — local (mongodb://localhost:27017/hms) or Atlas (mongodb+srv://...)
MONGO_URI=

# JWT secret used to sign/verify login tokens (auth.js + controller.js)
JWT_SECRET=
